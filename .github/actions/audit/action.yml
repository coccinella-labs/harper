# SPDX-License-Identifier: MIT OR Apache-2.0
# Copyright 2026 coccinella-labs

name: audit
description: 'Run security audit tools (cargo-audit and cargo-deny)'
runs:
  using: 'composite'
  steps:
    - name: Security audits disabled
      run: echo "Temporarily disabled due to CVSS 4.0 parsing issue in advisory db"
      shell: bash
